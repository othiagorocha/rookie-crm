[x] - Atualizar `app/layout.tsx` para envolver a aplicação com `ClerkProvider`, `ThemeProvider`, `QueryProvider` e `Toaster`.
  _Requirements 1.1, 1.4, 5.1
[x] - Ajustar `components/providers/query-provider.tsx` e criar `theme-provider.tsx` seguindo tokens de tema e suporte a dark mode.
  _Requirements 1.2, 1.3, 3.3
[x] - Criar pastas base (`constants/`, `hooks/`, `stores/`, `actions/`, `validations/`) e mover/organizar arquivos de layout em `components/layout/`.
  _Requirements 3.1, 3.2
[x] - Implementar `AppShell` com `Sidebar` e `Topbar`, aplicando paleta pink, navegação completa e versão mobile com drawer.
  _Requirements 2.1, 2.2, 2.3, 2.4, 3.2
[x] - Construir `stores/use-workspace-store.ts` controlando workspace ativo e ações de troca.
  _Requirements 4.2, 4.3
[x] - Integrar proteção de rotas autenticadas com Clerk no layout e preparar segmento público para propostas.
  _Requirements 4.1, 4.4
[x] - Configurar `react-hot-toast` global e handlers de erro exibindo mensagens amigáveis via `getErrorMessage`.
  _Requirements 5.1, 5.2
